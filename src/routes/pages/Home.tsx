import ProductsList from "../../components/ProductsList";
import ShopCart from "../../components/ShopCart";

const Home = () => {
    return (
        <div>
            <ProductsList />
            <ShopCart />
        </div>
    );
};

export default Home;
